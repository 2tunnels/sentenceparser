[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
textblob = ">=0.15.3,<1.0"
beautifulsoup4 = ">=4.7.1,<5.0"
lxml = ">=4.3.4,<5.0"

[dev-packages]
pytest = ">=4.6.3,<5.0"

[requires]
python_version = "3.7"
